# Data Model

```mermaid
classDiagram
    class TaskStatus {
      <<enumeration>>
      todo
      in-progress
      done
    }

    class Task {
        - id: int
        - description: string
        - status: TaskStatus
        - createdAt: datetime
        - updatedAt: datetime
    }

    class TaskManager {
        - repository: TaskRepository
        + create_task(description: string): void
        + update_task(task: Task, new_description: string?, new_status: string?): void
        + delete_task(id: int): void
        + list_tasks(filter: string?): void
    }

    class TaskRepository {
        - filename: string
        + save(task: Task): void
        + delete(id: int): void
        + find(id: int): Task
        + retrieve(status: string?): Task[]
    }

    class Command {
        <<abstract>>
        - task_manager: TaskManager
        + execute(): void*
    }

    class AddTaskCommand {
        - description: string
        + execute(): void
    }

    class UpdateTaskCommand {
        - description: string?
        - status: string?
        + execute(): void
    }
    
    class DeleteTaskCommand {
        - id: int
        + execute(): void
    }

    class ListTaskCommand {
        - filter: string?
        + execute(): void
    } 

    Task --> TaskStatus : has
    TaskManager --> TaskRepository : uses
    TaskManager ..> Task : manipulates
    TaskRepository ..> Task : persists
    
    Command <|.. AddTaskCommand
    Command <|.. UpdateTaskCommand
    Command <|.. DeleteTaskCommand
    Command <|.. ListTaskCommand

    AddTaskCommand --> TaskManager : uses
    UpdateTaskCommand --> TaskManager : uses
    DeleteTaskCommand --> TaskManager : uses
    ListTaskCommand --> TaskManager : uses
```

## Entity Description

### `Task`

Represents the user's tasks

- **Attributes:**
  - `id`: Unique identifier for the task.
  - `description`: Task detailed description.
  - `status`: current progress of the task, three options: 'todo', 'in-progress', 'done'
  - `createdAt`: exact date and time when the task was created.
  - `updatedAt`: exact date and time from the last update of the task.

### `TaskManager`

Manages all users tasks and applies business rules

- **Attributes:**
  - `repository`: Task repository for data persistance
- **Methods:**
  - `create_task()`: Creates a new task with users description, 'todo' status and a new identifier
  - `update_task()`: Updates an existing task status or description
  - `delete_task()`: Delete an existing task
  - `list_tasks()`: Show all tasks or by the specified filter

### `TaskRepository`

Persists the tasks on the database

- **Attributes:**
  - `filename`: JSON file reference
- **Methods:**
  - `save()`: Persists a task to the local database
  - `delete()`: Deletes a task from the local database by ID
  - `find()`: Searched for a specific task by ID
  - `retrieve()`: Returns all stored tasks or filter them by status

### `Command`

Realizes the users operations

- **Attributes:**
  - `task_manager`: Task manager reference for business application
- **Methods:**
  - `execute()`: Realize the commands intended function
